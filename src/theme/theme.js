"use client";
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#aa12bb",
    },
    // secondary: {
    //   main: "#ab32",
    // },
  },
});

export default theme;
